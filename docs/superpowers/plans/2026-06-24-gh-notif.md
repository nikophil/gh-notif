# gh-notif Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Une extension `gh` CLI (`gh notif`) qui affiche/pousse des notifications GitHub filtrées finement, avec liens directs et notifs desktop motivées.

**Architecture:** Exécutable Node zéro-dépendance. Les appels GitHub passent par `gh api` (héritage de `gh auth`). Cœur = `filter.js` (fonctions pures, testées sur fixtures). `collect.js` orchestre fetch+classify. `state.js` gère le dédup du `--watch`. `render.js`/`notify.js` produisent sortie terminal et notifs desktop.

**Tech Stack:** Node 24 (ESM), `node:test` natif, `gh api`, `notify-send`. Aucune dépendance npm.

## Global Constraints

- **Aucune dépendance npm.** `package.json` n'a pas de champ `dependencies`. Tout en built-ins Node.
- **ESM** : `package.json` a `"type": "module"`. Imports relatifs résolus via `import.meta.url` au besoin.
- **On ne traite QUE `subject.type === "PullRequest"`.** Tout le reste (Issue, Release, Discussion) → ignoré.
- **Aucune notif pour une mise à jour de PR** : push, nouveau commit, état CI, merge/close, label/assignee → toujours DROP, même sur mes propres PR.
- **Liste blanche de `reason`** : `review_requested, mention, team_mention, author, comment, subscribed, manual`. Tout autre `reason` → DROP.
- **Liens obligatoires** : chaque item rendu/notifié porte une URL `html_url` cliquable vers la cible précise (PR, ou ancre `#discussion_r…` / `#issuecomment_…`).
- **`gh api` injectable** : tout module appelant `gh` reçoit un *runner* en paramètre pour être testable sans réseau.

---

## File Structure

```
gh-notif/
├── gh-notif              # entrypoint exécutable (#!/usr/bin/env node) : parse args + dispatch
├── src/
│   ├── github.js         # makeGh(runner) → API GitHub via `gh api` (raw JSON)
│   ├── filter.js         # fonctions pures : classify(), findReplyToMe(), prHtmlUrl(), CATEGORY
│   ├── collect.js        # orchestration : inspectThread(), collectNotifications(), collectPending()
│   ├── state.js          # statePath/loadState/saveState/isNew/markSeen
│   ├── notify.js         # notifyMessage() + sendNotification() (notify-send, spawn injectable)
│   └── render.js         # renderList() — sortie terminal groupée avec liens
├── test/
│   ├── filter.test.js
│   ├── github.test.js
│   ├── collect.test.js
│   ├── state.test.js
│   ├── notify.test.js
│   └── render.test.js
├── package.json          # { "type": "module" }, scripts.test
└── README.md
```

### Shared type shapes (référence pour toutes les tâches)

```js
// Thread = objet brut renvoyé par GET /notifications
//   { id, reason, updated_at, subject:{ title, url, latest_comment_url, type }, repository:{ full_name } }
// Comment = objet brut review-comment ou issue-comment de l'API
//   { id, in_reply_to_id?, user:{ login }, html_url, created_at }
// Inspection = { latestComment: Comment|null, reviewComments: Comment[] }
// Item (sortie de classify) =
//   { category, actor, url, repo, number, title, threadId, updatedAt }
// PendingItem = { repo, number, title, url, updatedAt }
```

---

## Task 1: Scaffold du projet

**Files:**
- Create: `package.json`
- Create: `gh-notif`
- Create: `.gitignore`

**Interfaces:**
- Consumes: rien
- Produces: `npm test` exécutable ; `./gh-notif` exécutable affichant l'aide

- [ ] **Step 1: Créer `package.json`**

```json
{
  "name": "gh-notif",
  "version": "0.1.0",
  "type": "module",
  "description": "Extension gh CLI : notifications GitHub filtrées",
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: Créer `.gitignore`**

```
node_modules/
*.log
```

- [ ] **Step 3: Créer l'entrypoint `gh-notif` avec l'aide minimale**

```js
#!/usr/bin/env node

const HELP = `gh notif — notifications GitHub filtrées

Usage:
  gh notif            Liste les notifications pertinentes + reviews en attente
  gh notif --all      Inclut les notifications déjà lues
  gh notif --watch    Poll en continu et pousse des notifs desktop
  gh notif --help     Affiche cette aide
`;

function main(argv) {
  const args = new Set(argv.slice(2));
  if (args.has('--help') || args.has('-h')) {
    process.stdout.write(HELP);
    return;
  }
  process.stdout.write(HELP);
}

main(process.argv);
```

- [ ] **Step 4: Rendre l'entrypoint exécutable et vérifier**

Run: `chmod +x gh-notif && ./gh-notif --help`
Expected: l'aide s'affiche, exit 0.

- [ ] **Step 5: Commit**

```bash
git add package.json .gitignore gh-notif
git commit -m "feat: scaffold gh-notif (package.json, entrypoint, aide)"
```

---

## Task 2: Moteur de filtrage — `findReplyToMe`

**Files:**
- Create: `src/filter.js`
- Test: `test/filter.test.js`

**Interfaces:**
- Consumes: rien
- Produces: `findReplyToMe(reviewComments, me) → Comment|null` — renvoie le commentaire le plus récent, d'un autre auteur que `me`, situé dans un fil (chaîné par `in_reply_to_id`) où `me` a au moins un commentaire ; sinon `null`.

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/filter.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { findReplyToMe } from '../src/filter.js';

const ME = 'nikophil';

test('réponse sous mon commentaire → renvoie la réponse', () => {
  const comments = [
    { id: 1, user: { login: ME }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: 'alice' }, created_at: '2026-06-24T11:00:00Z', html_url: 'u2' },
  ];
  const r = findReplyToMe(comments, ME);
  assert.equal(r?.id, 2);
});

test('commentaire dans un fil où je ne participe pas → null', () => {
  const comments = [
    { id: 1, user: { login: 'bob' }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: 'alice' }, created_at: '2026-06-24T11:00:00Z', html_url: 'u2' },
  ];
  assert.equal(findReplyToMe(comments, ME), null);
});

test('seulement ma propre réponse → null', () => {
  const comments = [
    { id: 1, user: { login: 'alice' }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: ME }, created_at: '2026-06-24T11:00:00Z', html_url: 'u2' },
  ];
  assert.equal(findReplyToMe(comments, ME), null);
});

test('plusieurs réponses → renvoie la plus récente', () => {
  const comments = [
    { id: 1, user: { login: ME }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: 'alice' }, created_at: '2026-06-24T11:00:00Z', html_url: 'u2' },
    { id: 3, in_reply_to_id: 1, user: { login: 'bob' }, created_at: '2026-06-24T12:00:00Z', html_url: 'u3' },
  ];
  assert.equal(findReplyToMe(comments, ME)?.id, 3);
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/filter.test.js`
Expected: FAIL — `findReplyToMe is not a function` / import échoue.

- [ ] **Step 3: Implémenter `findReplyToMe`**

```js
// src/filter.js

// Regroupe les review-comments par fil (racine = remontée des in_reply_to_id),
// et renvoie le commentaire le plus récent d'un autre auteur dans un fil où `me`
// a participé. null si aucun.
export function findReplyToMe(reviewComments, me) {
  const byId = new Map(reviewComments.map((c) => [c.id, c]));
  const rootId = (c) => {
    let cur = c;
    while (cur.in_reply_to_id && byId.has(cur.in_reply_to_id)) cur = byId.get(cur.in_reply_to_id);
    return cur.id;
  };
  const threads = new Map();
  for (const c of reviewComments) {
    const r = rootId(c);
    if (!threads.has(r)) threads.set(r, []);
    threads.get(r).push(c);
  }
  let best = null;
  for (const comments of threads.values()) {
    const iParticipate = comments.some((c) => c.user?.login === me);
    if (!iParticipate) continue;
    for (const c of comments) {
      if (c.user?.login === me) continue;
      if (!best || c.created_at > best.created_at) best = c;
    }
  }
  return best;
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/filter.test.js`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/filter.js test/filter.test.js
git commit -m "feat: findReplyToMe — détection stricte de réponse dans mes fils"
```

---

## Task 3: Moteur de filtrage — `prHtmlUrl` et `classify`

**Files:**
- Modify: `src/filter.js`
- Test: `test/filter.test.js`

**Interfaces:**
- Consumes: `findReplyToMe` (Task 2)
- Produces:
  - `CATEGORY = { REVIEW_REQUEST, MENTION, ON_MY_PR, THREAD_REPLY }` (valeurs string)
  - `prHtmlUrl(thread) → string` — `https://github.com/{full_name}/pull/{num}` (num = dernier segment de `subject.url`)
  - `classify(thread, me, inspection) → Item|null` — applique toutes les règles de filtrage. `inspection` peut être `null` pour les `reason` qui n'en ont pas besoin.

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/filter.test.js — ajouter
import { classify, prHtmlUrl, CATEGORY } from '../src/filter.js';

const prThread = (over = {}) => ({
  id: 't1',
  reason: 'review_requested',
  updated_at: '2026-06-24T12:00:00Z',
  subject: { title: 'Ma PR', url: 'https://api.github.com/repos/o/r/pulls/42', latest_comment_url: null, type: 'PullRequest' },
  repository: { full_name: 'o/r' },
  ...over,
});

test('prHtmlUrl construit l’URL web', () => {
  assert.equal(prHtmlUrl(prThread()), 'https://github.com/o/r/pull/42');
});

test('non-PullRequest → null', () => {
  const t = prThread({ subject: { ...prThread().subject, type: 'Issue' } });
  assert.equal(classify(t, ME, null), null);
});

test('reason hors liste blanche (ci_activity) → null', () => {
  assert.equal(classify(prThread({ reason: 'ci_activity' }), ME, null), null);
});

test('review_requested → REVIEW_REQUEST avec URL PR', () => {
  const item = classify(prThread({ reason: 'review_requested' }), ME, null);
  assert.equal(item.category, CATEGORY.REVIEW_REQUEST);
  assert.equal(item.url, 'https://github.com/o/r/pull/42');
  assert.equal(item.repo, 'o/r');
  assert.equal(item.number, 42);
  assert.equal(item.title, 'Ma PR');
  assert.equal(item.threadId, 't1');
  assert.equal(item.updatedAt, '2026-06-24T12:00:00Z');
});

test('mention → MENTION avec auteur + URL du commentaire', () => {
  const insp = { latestComment: { user: { login: 'alice' }, html_url: 'https://github.com/o/r/pull/42#discussion_r9' }, reviewComments: [] };
  const item = classify(prThread({ reason: 'mention' }), ME, insp);
  assert.equal(item.category, CATEGORY.MENTION);
  assert.equal(item.actor, 'alice');
  assert.equal(item.url, 'https://github.com/o/r/pull/42#discussion_r9');
});

test('author avec commentaire d’un autre → ON_MY_PR', () => {
  const insp = { latestComment: { user: { login: 'bob' }, html_url: 'https://github.com/o/r/pull/42#issuecomment_5' }, reviewComments: [] };
  const item = classify(prThread({ reason: 'author' }), ME, insp);
  assert.equal(item.category, CATEGORY.ON_MY_PR);
  assert.equal(item.actor, 'bob');
});

test('author SANS nouveau commentaire (push/CI/merge) → null', () => {
  const insp = { latestComment: null, reviewComments: [] };
  assert.equal(classify(prThread({ reason: 'author' }), ME, insp), null);
});

test('author mais dernier acteur = moi → null', () => {
  const insp = { latestComment: { user: { login: ME }, html_url: 'x' }, reviewComments: [] };
  assert.equal(classify(prThread({ reason: 'author' }), ME, insp), null);
});

test('comment avec réponse à mon fil → THREAD_REPLY', () => {
  const insp = { latestComment: null, reviewComments: [
    { id: 1, user: { login: ME }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: 'carol' }, created_at: '2026-06-24T11:00:00Z', html_url: 'https://github.com/o/r/pull/42#discussion_r2' },
  ] };
  const item = classify(prThread({ reason: 'comment' }), ME, insp);
  assert.equal(item.category, CATEGORY.THREAD_REPLY);
  assert.equal(item.actor, 'carol');
  assert.equal(item.url, 'https://github.com/o/r/pull/42#discussion_r2');
});

test('comment sur PR où je suis juste reviewer (pas de fil à moi) → null', () => {
  const insp = { latestComment: null, reviewComments: [
    { id: 1, user: { login: 'bob' }, created_at: '2026-06-24T10:00:00Z', html_url: 'u1' },
    { id: 2, in_reply_to_id: 1, user: { login: 'alice' }, created_at: '2026-06-24T11:00:00Z', html_url: 'u2' },
  ] };
  assert.equal(classify(prThread({ reason: 'comment' }), ME, insp), null);
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/filter.test.js`
Expected: FAIL — `classify`/`prHtmlUrl`/`CATEGORY` non définis.

- [ ] **Step 3: Implémenter `prHtmlUrl`, `CATEGORY`, `classify`**

```js
// src/filter.js — ajouter en haut du fichier

export const CATEGORY = {
  REVIEW_REQUEST: 'review_request',
  MENTION: 'mention',
  ON_MY_PR: 'on_my_pr',
  THREAD_REPLY: 'thread_reply',
};

const ALLOWED_REASONS = new Set([
  'review_requested', 'mention', 'team_mention',
  'author', 'comment', 'subscribed', 'manual',
]);

function prNumber(thread) {
  return Number(thread.subject.url.split('/').pop());
}

export function prHtmlUrl(thread) {
  return `https://github.com/${thread.repository.full_name}/pull/${prNumber(thread)}`;
}

function baseItem(thread, extra) {
  return {
    repo: thread.repository.full_name,
    number: prNumber(thread),
    title: thread.subject.title,
    threadId: thread.id,
    updatedAt: thread.updated_at,
    ...extra,
  };
}

export function classify(thread, me, inspection) {
  if (thread.subject?.type !== 'PullRequest') return null;
  const reason = thread.reason;
  if (!ALLOWED_REASONS.has(reason)) return null;

  if (reason === 'review_requested') {
    return baseItem(thread, { category: CATEGORY.REVIEW_REQUEST, actor: null, url: prHtmlUrl(thread) });
  }

  if (reason === 'mention' || reason === 'team_mention') {
    const c = inspection?.latestComment;
    return baseItem(thread, {
      category: CATEGORY.MENTION,
      actor: c?.user?.login ?? null,
      url: c?.html_url ?? prHtmlUrl(thread),
    });
  }

  if (reason === 'author') {
    const c = inspection?.latestComment;
    if (!c) return null;                       // pas de nouveau commentaire => mise à jour de PR
    if (c.user?.login === me) return null;     // ma propre action
    return baseItem(thread, { category: CATEGORY.ON_MY_PR, actor: c.user.login, url: c.html_url });
  }

  // comment / subscribed / manual
  const reply = findReplyToMe(inspection?.reviewComments ?? [], me);
  if (!reply) return null;
  return baseItem(thread, { category: CATEGORY.THREAD_REPLY, actor: reply.user.login, url: reply.html_url });
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/filter.test.js`
Expected: PASS (tous les tests filter).

- [ ] **Step 5: Commit**

```bash
git add src/filter.js test/filter.test.js
git commit -m "feat: classify — règles de filtrage complètes par reason"
```

---

## Task 4: Wrapper API GitHub — `github.js`

**Files:**
- Create: `src/github.js`
- Test: `test/github.test.js`

**Interfaces:**
- Consumes: rien
- Produces: `makeGh(runner) → gh` où `runner(args:string[]) → Promise<string>` (stdout). `gh` expose :
  - `getCurrentUser() → Promise<string>`
  - `listNotifications({ all }) → Promise<Thread[]>`
  - `getComment(apiUrl) → Promise<Comment|null>`
  - `getReviewComments(repoFullName, number) → Promise<Comment[]>`
  - `searchReviewRequested() → Promise<SearchItem[]>` (items bruts `search/issues`)

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/github.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { makeGh } from '../src/github.js';

function fakeRunner(map) {
  const calls = [];
  const run = async (args) => {
    calls.push(args);
    const key = args.join(' ');
    for (const [match, out] of map) if (key.includes(match)) return out;
    throw new Error('no stub for ' + key);
  };
  run.calls = calls;
  return run;
}

test('getCurrentUser renvoie le login', async () => {
  const gh = makeGh(fakeRunner([['api user', JSON.stringify({ login: 'nikophil' })]]));
  assert.equal(await gh.getCurrentUser(), 'nikophil');
});

test('listNotifications parse le tableau et passe all=true', async () => {
  const runner = fakeRunner([['/notifications', JSON.stringify([{ id: '1' }])]]);
  const gh = makeGh(runner);
  const out = await gh.listNotifications({ all: true });
  assert.equal(out[0].id, '1');
  assert.ok(runner.calls[0].join(' ').includes('all=true'));
});

test('getComment renvoie null sur stdout vide', async () => {
  const gh = makeGh(fakeRunner([['repos/o/r', '']]));
  assert.equal(await gh.getComment('https://api.github.com/repos/o/r/issues/comments/1'), null);
});

test('getReviewComments construit le bon chemin', async () => {
  const runner = fakeRunner([['/pulls/42/comments', JSON.stringify([{ id: 1 }])]]);
  const gh = makeGh(runner);
  const out = await gh.getReviewComments('o/r', 42);
  assert.equal(out[0].id, 1);
  assert.ok(runner.calls[0].join(' ').includes('repos/o/r/pulls/42/comments'));
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/github.test.js`
Expected: FAIL — `makeGh` non défini.

- [ ] **Step 3: Implémenter `github.js`**

```js
// src/github.js
import { execFile } from 'node:child_process';
import { promisify } from 'node:util';

const pexec = promisify(execFile);

async function defaultRunner(args) {
  const { stdout } = await pexec('gh', args, { maxBuffer: 64 * 1024 * 1024 });
  return stdout;
}

function parseJson(stdout) {
  const s = stdout.trim();
  if (!s) return null;
  return JSON.parse(s);
}

export function makeGh(runner = defaultRunner) {
  return {
    async getCurrentUser() {
      return parseJson(await runner(['api', 'user'])).login;
    },
    async listNotifications({ all = false } = {}) {
      const args = ['api', '--paginate', '/notifications'];
      if (all) args.push('-f', 'all=true');
      return parseJson(await runner(args)) ?? [];
    },
    async getComment(apiUrl) {
      const path = apiUrl.replace('https://api.github.com', '');
      return parseJson(await runner(['api', path]));
    },
    async getReviewComments(repoFullName, number) {
      return parseJson(await runner(['api', '--paginate', `repos/${repoFullName}/pulls/${number}/comments`])) ?? [];
    },
    async searchReviewRequested() {
      const out = parseJson(await runner(['api', '-X', 'GET', 'search/issues', '-f', 'q=is:open is:pr review-requested:@me']));
      return out?.items ?? [];
    },
  };
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/github.test.js`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/github.js test/github.test.js
git commit -m "feat: github.js — wrapper gh api injectable"
```

---

## Task 5: Orchestration — `collect.js`

**Files:**
- Create: `src/collect.js`
- Test: `test/collect.test.js`

**Interfaces:**
- Consumes: `classify` (Task 3), un objet `gh` (Task 4)
- Produces:
  - `inspectThread(gh, thread, me) → Promise<Inspection|null>` — décide quoi fetcher selon `reason` : `review_requested` → `null` ; `mention`/`team_mention`/`author` → `{ latestComment, reviewComments: [] }` via `getComment(latest_comment_url)` ; `comment`/`subscribed`/`manual` → `{ latestComment: null, reviewComments }` via `getReviewComments`.
  - `collectNotifications(gh, me, { all }) → Promise<Item[]>` — `listNotifications` → `inspectThread` → `classify`, filtre les `null`.
  - `collectPending(gh) → Promise<PendingItem[]>` — `searchReviewRequested` → mappe en `PendingItem`.

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/collect.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { collectNotifications, collectPending } from '../src/collect.js';

const ME = 'nikophil';

function fakeGh(over = {}) {
  return {
    async getCurrentUser() { return ME; },
    async listNotifications() { return over.notifications ?? []; },
    async getComment() { return over.comment ?? null; },
    async getReviewComments() { return over.reviewComments ?? []; },
    async searchReviewRequested() { return over.search ?? []; },
  };
}

const reviewReqThread = {
  id: 't1', reason: 'review_requested', updated_at: '2026-06-24T12:00:00Z',
  subject: { title: 'PR A', url: 'https://api.github.com/repos/o/r/pulls/42', latest_comment_url: null, type: 'PullRequest' },
  repository: { full_name: 'o/r' },
};

test('collectNotifications garde une review demandée', async () => {
  const items = await collectNotifications(fakeGh({ notifications: [reviewReqThread] }), ME, {});
  assert.equal(items.length, 1);
  assert.equal(items[0].category, 'review_request');
});

test('collectNotifications drop un thread non-PR', async () => {
  const issue = { ...reviewReqThread, subject: { ...reviewReqThread.subject, type: 'Issue' } };
  const items = await collectNotifications(fakeGh({ notifications: [issue] }), ME, {});
  assert.equal(items.length, 0);
});

test('collectPending mappe les items de recherche', async () => {
  const search = [{
    number: 98, title: 'PR à review',
    html_url: 'https://github.com/o/r/pull/98',
    updated_at: '2026-06-20T09:00:00Z',
    repository_url: 'https://api.github.com/repos/o/r',
  }];
  const pending = await collectPending(fakeGh({ search }));
  assert.deepEqual(pending[0], {
    repo: 'o/r', number: 98, title: 'PR à review',
    url: 'https://github.com/o/r/pull/98', updatedAt: '2026-06-20T09:00:00Z',
  });
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/collect.test.js`
Expected: FAIL — exports non définis.

- [ ] **Step 3: Implémenter `collect.js`**

```js
// src/collect.js
import { classify } from './filter.js';

export async function inspectThread(gh, thread, me) {
  const reason = thread.reason;
  if (reason === 'review_requested') return null;

  if (reason === 'mention' || reason === 'team_mention' || reason === 'author') {
    const url = thread.subject?.latest_comment_url;
    const latestComment = url ? await gh.getComment(url) : null;
    return { latestComment, reviewComments: [] };
  }

  // comment / subscribed / manual
  const number = Number(thread.subject.url.split('/').pop());
  const reviewComments = await gh.getReviewComments(thread.repository.full_name, number);
  return { latestComment: null, reviewComments };
}

export async function collectNotifications(gh, me, { all = false } = {}) {
  const threads = await gh.listNotifications({ all });
  const items = [];
  for (const thread of threads) {
    if (thread.subject?.type !== 'PullRequest') continue; // évite tout fetch inutile
    const inspection = await inspectThread(gh, thread, me);
    const item = classify(thread, me, inspection);
    if (item) items.push(item);
  }
  return items;
}

export async function collectPending(gh) {
  const items = await gh.searchReviewRequested();
  return items.map((it) => ({
    repo: it.repository_url.replace('https://api.github.com/repos/', ''),
    number: it.number,
    title: it.title,
    url: it.html_url,
    updatedAt: it.updated_at,
  }));
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/collect.test.js`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/collect.js test/collect.test.js
git commit -m "feat: collect.js — orchestration fetch + classify"
```

---

## Task 6: État & dédup — `state.js`

**Files:**
- Create: `src/state.js`
- Test: `test/state.test.js`

**Interfaces:**
- Consumes: rien
- Produces:
  - `statePath() → string` — `${XDG_STATE_HOME ?? ~/.local/state}/gh-notif/seen.json`
  - `loadState(path) → object` — `{}` si absent/illisible
  - `saveState(path, state) → void` — crée le dossier au besoin
  - `isNew(state, item) → boolean` — `true` si `item.updatedAt > state[item.threadId]` (ou clé absente)
  - `markSeen(state, item) → void` — mute `state[item.threadId] = item.updatedAt`

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/state.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { rmSync, mkdtempSync } from 'node:fs';
import { loadState, saveState, isNew, markSeen, statePath } from '../src/state.js';

test('statePath respecte XDG_STATE_HOME', () => {
  const prev = process.env.XDG_STATE_HOME;
  process.env.XDG_STATE_HOME = '/xdg';
  assert.equal(statePath(), '/xdg/gh-notif/seen.json');
  if (prev === undefined) delete process.env.XDG_STATE_HOME; else process.env.XDG_STATE_HOME = prev;
});

test('loadState renvoie {} si fichier absent', () => {
  assert.deepEqual(loadState('/nope/nope/seen.json'), {});
});

test('save puis load round-trip', () => {
  const dir = mkdtempSync(join(tmpdir(), 'ghnotif-'));
  const p = join(dir, 'sub', 'seen.json');
  saveState(p, { t1: '2026-06-24T10:00:00Z' });
  assert.deepEqual(loadState(p), { t1: '2026-06-24T10:00:00Z' });
  rmSync(dir, { recursive: true, force: true });
});

test('isNew: clé absente → true', () => {
  assert.equal(isNew({}, { threadId: 't1', updatedAt: '2026-06-24T10:00:00Z' }), true);
});

test('isNew: updatedAt plus récent → true, identique → false', () => {
  const state = { t1: '2026-06-24T10:00:00Z' };
  assert.equal(isNew(state, { threadId: 't1', updatedAt: '2026-06-24T11:00:00Z' }), true);
  assert.equal(isNew(state, { threadId: 't1', updatedAt: '2026-06-24T10:00:00Z' }), false);
});

test('markSeen écrit la valeur', () => {
  const state = {};
  markSeen(state, { threadId: 't1', updatedAt: '2026-06-24T10:00:00Z' });
  assert.equal(state.t1, '2026-06-24T10:00:00Z');
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/state.test.js`
Expected: FAIL — exports non définis.

- [ ] **Step 3: Implémenter `state.js`**

```js
// src/state.js
import { homedir } from 'node:os';
import { join, dirname } from 'node:path';
import { readFileSync, writeFileSync, mkdirSync } from 'node:fs';

export function statePath() {
  const base = process.env.XDG_STATE_HOME || join(homedir(), '.local', 'state');
  return join(base, 'gh-notif', 'seen.json');
}

export function loadState(path) {
  try {
    return JSON.parse(readFileSync(path, 'utf8'));
  } catch {
    return {};
  }
}

export function saveState(path, state) {
  mkdirSync(dirname(path), { recursive: true });
  writeFileSync(path, JSON.stringify(state, null, 2));
}

export function isNew(state, item) {
  const seen = state[item.threadId];
  return !seen || item.updatedAt > seen;
}

export function markSeen(state, item) {
  state[item.threadId] = item.updatedAt;
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/state.test.js`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/state.js test/state.test.js
git commit -m "feat: state.js — persistance et dédup du watch"
```

---

## Task 7: Notifs desktop — `notify.js`

**Files:**
- Create: `src/notify.js`
- Test: `test/notify.test.js`

**Interfaces:**
- Consumes: `CATEGORY` (Task 3)
- Produces:
  - `notifyMessage(item) → { title, body }` — titre = motif (voir spec), corps = `repo #num — titre\nurl`
  - `sendNotification(item, spawn) → void` — appelle `spawn('notify-send', [title, body])` ; `spawn` injectable (défaut = `child_process.spawn`)

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/notify.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { CATEGORY } from '../src/filter.js';
import { notifyMessage, sendNotification } from '../src/notify.js';

const base = { repo: 'o/r', number: 42, title: 'Ma PR', url: 'https://github.com/o/r/pull/42' };

test('titre review demandée', () => {
  const m = notifyMessage({ ...base, category: CATEGORY.REVIEW_REQUEST, actor: null });
  assert.equal(m.title, 'Nouvelle PR à review');
  assert.ok(m.body.includes('o/r #42'));
  assert.ok(m.body.includes('https://github.com/o/r/pull/42'));
});

test('titre mention inclut l’auteur', () => {
  const m = notifyMessage({ ...base, category: CATEGORY.MENTION, actor: 'alice' });
  assert.equal(m.title, '@alice t’a mentionné');
});

test('titre activité sur ma PR', () => {
  const m = notifyMessage({ ...base, category: CATEGORY.ON_MY_PR, actor: 'bob' });
  assert.equal(m.title, '@bob a commenté ta PR');
});

test('titre réponse', () => {
  const m = notifyMessage({ ...base, category: CATEGORY.THREAD_REPLY, actor: 'carol' });
  assert.equal(m.title, '@carol t’a répondu');
});

test('sendNotification appelle spawn avec titre et corps', () => {
  const calls = [];
  const spawn = (cmd, args) => { calls.push({ cmd, args }); return { on() {}, unref() {} }; };
  sendNotification({ ...base, category: CATEGORY.REVIEW_REQUEST, actor: null }, spawn);
  assert.equal(calls[0].cmd, 'notify-send');
  assert.equal(calls[0].args[0], 'Nouvelle PR à review');
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/notify.test.js`
Expected: FAIL — exports non définis.

- [ ] **Step 3: Implémenter `notify.js`**

```js
// src/notify.js
import { spawn as nodeSpawn } from 'node:child_process';
import { CATEGORY } from './filter.js';

export function notifyMessage(item) {
  let title;
  switch (item.category) {
    case CATEGORY.REVIEW_REQUEST: title = 'Nouvelle PR à review'; break;
    case CATEGORY.MENTION:        title = `@${item.actor} t’a mentionné`; break;
    case CATEGORY.ON_MY_PR:       title = `@${item.actor} a commenté ta PR`; break;
    case CATEGORY.THREAD_REPLY:   title = `@${item.actor} t’a répondu`; break;
    default:                      title = 'Notification';
  }
  const body = `${item.repo} #${item.number} — ${item.title}\n${item.url}`;
  return { title, body };
}

export function sendNotification(item, spawn = nodeSpawn) {
  const { title, body } = notifyMessage(item);
  const child = spawn('notify-send', [title, body], { stdio: 'ignore' });
  if (child.unref) child.unref();
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/notify.test.js`
Expected: PASS (5 tests).

- [ ] **Step 5: Commit**

```bash
git add src/notify.js test/notify.test.js
git commit -m "feat: notify.js — notifs desktop avec motif dans le titre"
```

---

## Task 8: Rendu terminal — `render.js`

**Files:**
- Create: `src/render.js`
- Test: `test/render.test.js`

**Interfaces:**
- Consumes: `CATEGORY` (Task 3)
- Produces: `renderList(items, pending) → string` — sortie groupée par catégorie (ordre : review demandée, mention, activité sur mes PR, réponse) avec compteur et liens, puis un séparateur et la section « Reviews en attente ». Catégories vides omises. Si tout est vide → message « Rien à signaler ✨ ».

- [ ] **Step 1: Écrire les tests qui échouent**

```js
// test/render.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { CATEGORY } from '../src/filter.js';
import { renderList } from '../src/render.js';

test('rendu vide', () => {
  assert.match(renderList([], []), /Rien à signaler/);
});

test('rend une review demandée avec lien', () => {
  const items = [{ category: CATEGORY.REVIEW_REQUEST, repo: 'o/r', number: 42, title: 'PR A', url: 'https://github.com/o/r/pull/42', actor: null }];
  const out = renderList(items, []);
  assert.match(out, /Reviews demandées \(1\)/);
  assert.match(out, /o\/r #42/);
  assert.match(out, /https:\/\/github.com\/o\/r\/pull\/42/);
});

test('rend la section reviews en attente', () => {
  const pending = [{ repo: 'o/r', number: 98, title: 'PR X', url: 'https://github.com/o/r/pull/98', updatedAt: '2026-06-20T09:00:00Z' }];
  const out = renderList([], pending);
  assert.match(out, /Reviews en attente \(1\)/);
  assert.match(out, /o\/r #98/);
});

test('omet les catégories vides', () => {
  const items = [{ category: CATEGORY.MENTION, repo: 'o/r', number: 1, title: 'T', url: 'u', actor: 'alice' }];
  const out = renderList(items, []);
  assert.doesNotMatch(out, /Reviews demandées/);
  assert.match(out, /Mentions \(1\)/);
});
```

- [ ] **Step 2: Lancer les tests, vérifier l'échec**

Run: `node --test test/render.test.js`
Expected: FAIL — `renderList` non défini.

- [ ] **Step 3: Implémenter `render.js`**

```js
// src/render.js
import { CATEGORY } from './filter.js';

const SECTIONS = [
  { category: CATEGORY.REVIEW_REQUEST, icon: '🔍', label: 'Reviews demandées' },
  { category: CATEGORY.MENTION,        icon: '💬', label: 'Mentions' },
  { category: CATEGORY.ON_MY_PR,       icon: '📥', label: 'Activité sur tes PR' },
  { category: CATEGORY.THREAD_REPLY,   icon: '↩️', label: 'Réponses à tes commentaires' },
];

function suffix(item) {
  switch (item.category) {
    case CATEGORY.MENTION:      return `  — mention de @${item.actor}`;
    case CATEGORY.ON_MY_PR:     return `  — @${item.actor} a commenté`;
    case CATEGORY.THREAD_REPLY: return `  — @${item.actor} t’a répondu`;
    default:                    return '';
  }
}

function renderItem(item) {
  return `  ${item.repo} #${item.number}  ${item.title}${suffix(item)}\n  → ${item.url}`;
}

export function renderList(items, pending) {
  const blocks = [];
  for (const section of SECTIONS) {
    const group = items.filter((i) => i.category === section.category);
    if (group.length === 0) continue;
    blocks.push(`${section.icon} ${section.label} (${group.length})\n${group.map(renderItem).join('\n')}`);
  }
  if (pending.length > 0) {
    const lines = pending.map((p) => `  ${p.repo} #${p.number}  ${p.title}\n  → ${p.url}`).join('\n');
    blocks.push(`${'─'.repeat(30)}\n📋 Reviews en attente (${pending.length})\n${lines}`);
  }
  if (blocks.length === 0) return 'Rien à signaler ✨\n';
  return blocks.join('\n\n') + '\n';
}
```

- [ ] **Step 4: Lancer les tests, vérifier le succès**

Run: `node --test test/render.test.js`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/render.js test/render.test.js
git commit -m "feat: render.js — sortie terminal groupée avec liens"
```

---

## Task 9: Commande `gh notif` (list) dans l'entrypoint

**Files:**
- Modify: `gh-notif`

**Interfaces:**
- Consumes: `makeGh` (Task 4), `collectNotifications`/`collectPending` (Task 5), `renderList` (Task 8)
- Produces: `gh notif` et `gh notif --all` affichent la liste rendue

- [ ] **Step 1: Câbler la commande list**

```js
#!/usr/bin/env node
import { makeGh } from './src/github.js';
import { collectNotifications, collectPending } from './src/collect.js';
import { renderList } from './src/render.js';

const HELP = `gh notif — notifications GitHub filtrées

Usage:
  gh notif            Liste les notifications pertinentes + reviews en attente
  gh notif --all      Inclut les notifications déjà lues
  gh notif --watch    Poll en continu et pousse des notifs desktop
  gh notif --help     Affiche cette aide
`;

async function runList(gh, { all }) {
  const me = await gh.getCurrentUser();
  const [items, pending] = await Promise.all([
    collectNotifications(gh, me, { all }),
    collectPending(gh),
  ]);
  process.stdout.write(renderList(items, pending));
}

async function main(argv) {
  const args = new Set(argv.slice(2));
  if (args.has('--help') || args.has('-h')) { process.stdout.write(HELP); return; }
  const gh = makeGh();
  try {
    await runList(gh, { all: args.has('--all') });
  } catch (err) {
    process.stderr.write(`gh notif: ${err.message}\n`);
    process.exit(1);
  }
}

main(process.argv);
```

- [ ] **Step 2: Vérifier que les imports relatifs résolvent**

Le fichier `gh-notif` est à la racine, `src/` est à côté. Les imports `./src/...` résolvent par rapport au fichier. Vérifier qu'il n'y a pas d'erreur d'import au lancement de l'aide :

Run: `./gh-notif --help`
Expected: l'aide s'affiche (pas d'erreur d'import ESM).

- [ ] **Step 3: Smoke test réel contre GitHub**

Run: `./gh-notif`
Expected: soit `Rien à signaler ✨`, soit des sections avec des liens. Pas de crash. (Test manuel ; dépend de l'état réel du compte.)

- [ ] **Step 4: Commit**

```bash
git add gh-notif
git commit -m "feat: commande gh notif (list) + section reviews en attente"
```

---

## Task 10: Commande `gh notif --watch`

**Files:**
- Modify: `gh-notif`

**Interfaces:**
- Consumes: `collectNotifications` (Task 5), `state.js` (Task 6), `sendNotification` (Task 7)
- Produces: `gh notif --watch` — boucle infinie : collecte, pousse les items nouveaux (via état), persiste, dort `interval` secondes (défaut 60).

- [ ] **Step 1: Ajouter la boucle watch à l'entrypoint**

```js
// gh-notif — ajouter ces imports en haut
import { sendNotification } from './src/notify.js';
import { statePath, loadState, saveState, isNew, markSeen } from './src/state.js';

const POLL_SECONDS = 60;
const sleep = (s) => new Promise((r) => setTimeout(r, s * 1000));

async function runWatch(gh) {
  const me = await gh.getCurrentUser();
  const path = statePath();
  const state = loadState(path);
  process.stdout.write(`gh notif --watch : surveillance toutes les ${POLL_SECONDS}s (Ctrl-C pour arrêter)\n`);
  for (;;) {
    try {
      const items = await collectNotifications(gh, me, { all: false });
      const fresh = items.filter((i) => isNew(state, i));
      for (const item of fresh) {
        sendNotification(item);
        markSeen(state, item);
      }
      if (fresh.length > 0) saveState(path, state);
    } catch (err) {
      process.stderr.write(`gh notif --watch: ${err.message}\n`);
    }
    await sleep(POLL_SECONDS);
  }
}
```

- [ ] **Step 2: Brancher `--watch` dans `main`**

Dans `main`, remplacer le bloc `try` par un dispatch :

```js
  const gh = makeGh();
  try {
    if (args.has('--watch')) {
      await runWatch(gh);
    } else {
      await runList(gh, { all: args.has('--all') });
    }
  } catch (err) {
    process.stderr.write(`gh notif: ${err.message}\n`);
    process.exit(1);
  }
```

- [ ] **Step 3: Smoke test du watch**

Run: `timeout 5 ./gh-notif --watch; echo "exit=$?"`
Expected: affiche la ligne de surveillance, tourne ~5s sans crash, puis `exit=124` (timeout). Si une notif réelle arrive, un `notify-send` apparaît.

- [ ] **Step 4: Commit**

```bash
git add gh-notif
git commit -m "feat: gh notif --watch — poll + notifs desktop dédupliquées"
```

---

## Task 11: README, install de l'extension, tests complets

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: tout ce qui précède
- Produces: extension installée localement (`gh extension install .`) et utilisable via `gh notif`

- [ ] **Step 1: Écrire le `README.md`**

````markdown
# gh-notif

Extension `gh` CLI : notifications GitHub filtrées finement.

Notifie uniquement pour : reviews demandées, mentions, activité d'autrui sur **mes** PR,
et réponses à **mes** commentaires. Ignore les mises à jour de PR (push/CI/merge) et le bruit
des PR où je suis simple reviewer.

## Prérequis
- `gh` authentifié (`gh auth login`)
- Node ≥ 18
- `notify-send` (paquet `libnotify-bin`) pour les notifs desktop

## Installation
```bash
gh extension install .
# ou depuis le repo distant :
# gh extension install nikophil/gh-notif
```

## Usage
```bash
gh notif            # liste filtrée + reviews en attente
gh notif --all      # inclut les notifications déjà lues
gh notif --watch    # surveille et pousse des notifs desktop (toutes les 60s)
```

## Tests
```bash
npm test
```
````

- [ ] **Step 2: Lancer toute la suite de tests**

Run: `npm test`
Expected: PASS, tous les fichiers `test/*.test.js` verts.

- [ ] **Step 3: Installer l'extension localement et vérifier**

Run: `gh extension install . && gh notif --help`
Expected: l'aide de `gh notif` s'affiche via la commande `gh`.

- [ ] **Step 4: Smoke test final via `gh`**

Run: `gh notif`
Expected: liste réelle ou `Rien à signaler ✨`, sans crash.

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "docs: README + instructions d'installation de l'extension"
```

---

## Self-Review

**Spec coverage :**
- Notifs review demandée → Task 3 (`review_requested`), rendu Task 8, notif Task 7. ✓
- Mentions (PR only) → Task 3 + garde `subject.type` Task 3/5. ✓
- Activité d'autrui sur mes PR → Task 3 (`author`, dernier acteur ≠ moi, commentaire présent). ✓
- Réponse à mes commentaires (strict, niveau fil) → Task 2 + Task 3. ✓
- Pas de notif pour mise à jour de PR → Task 3 (liste blanche + `author` sans commentaire = DROP). ✓
- Pas de notif PR où je suis simple reviewer → Task 2/3 (`comment` sans fil à moi = DROP). ✓
- Reviews en attente affichées par défaut → Task 5 (`collectPending`) + Task 8 (section) + Task 9. ✓
- Liens directs → Task 3 (urls), Task 8 (rendu), Task 7 (corps notif). ✓
- Notifs desktop motivées → Task 7 (`notifyMessage`). ✓
- `gh notif` / `--all` / `--watch` → Tasks 9, 10. ✓
- Dédup watch → Task 6 + Task 10. ✓
- Gestion d'erreurs (gh absent, watch continue) → Task 9/10 (try/catch). ✓
- Zéro dépendance, ESM, `node:test` → Task 1 + global constraints. ✓

**Placeholder scan :** aucun TBD/TODO ; tout le code est complet. ✓

**Type consistency :** `Item` ({category, actor, url, repo, number, title, threadId, updatedAt}) produit par `classify` (Task 3) et consommé par `render.js` (Task 8), `notify.js` (Task 7), `state.js` (Task 6, via threadId/updatedAt). `Inspection` ({latestComment, reviewComments}) produit par `inspectThread` (Task 5) et consommé par `classify` (Task 3). `makeGh` runner-injectable cohérent Tasks 4/5. ✓
